<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../../../partials/head') %>
    <title>Sessions</title>
    <link rel="stylesheet" href="/styles/listStyle.css"> 
</head>
<body>
    <div class="container">
        <%- include('../../../partials/sidebar') %>
        <section>
            <%- include('../../../partials/header') %>
            <div class="heading">
                <h1>Liste des sessions</h1>
            </div>
            <table>
                <thead>
                    <tr>
                        <th>Professeur</th>
                        <th>Filière</th>
                        <th>Module</th>
                        <th>Session</th>
                        <th>Date de création</th>
                    </tr>
                </thead>
                <tbody>
                    <% absenceLists.forEach(absencelist => { %>
                        <tr>
                            <td><%= absencelist.username %></td>
                            <td><%= absencelist.sector %></td>
                            <td><%= absencelist.module %></td>
                            <td><%= absencelist.session %></td>
                            
                            <td><%= absencelist.createdAt.toLocaleString().split(',')[0] %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </section>
    </div>
</body>
</html>